Bug-Prioritäten:
    [1] Kritisch:    Muss behoben werden, bevor die Anwendung ernsthaft verwendet werden kann
    [2] Störend:     Sollte behoben werden, bevor die Anwendung ernsthaft verwendet werden kann
    [3] Unauffällig: Sollte behoben werden, fällt bei der Verwendung aber kaum bis garnicht auf
    [S] Surface:     Surface-spezifisches Problem (betrifft eventuell alle Geräte mit anderer Auflösung)

#######################################################################################################################################################

BUGS:
    -GUI:
        -[1] ?? Exception (Not on JavaFX-Thread) beim enablen/disablen der Tabelle
        -[2] [FIXED?] Keine "leeren" Werte den Comboboxen  des EditFilePresenters hinzufügen
        -[2] Gesamtdauer der aktuellen Wiedergabe muss aktualisiert werden, wenn eine noch nicht geladene Datei geladen wird
            (erledigt sich vermutlich von selbst während den Änderungen an der Lademechanik -> Laden, wenn angeklickt)
        -[2] Filterergebnisse aktualisieren sich nicht während dem Laden (überhaupt möglich?)
            -Evtl. zumindest bei auch bei Enter nochmal filtern (onAction -> setPredicate)
        -[3] Tabellen-Platzhalter (u.A. für 0 Filterergebnisse)
        -[3] Fokus-Wechsel über TAB funktioniert nicht richtig

          (Mit aktueller Lademechanik kein Problem mehr)
        -[?] Gelegentlich auftretende Concurrent Modifications Exception beim hinzufügen der Dateien in die Tabelle.
                Hat aber auf dem ersten Blick keine negativen Auswirkungen(?) (Mit höherem Tabellenintervall(10) gelöst?)
        -[?] Gelegentlich fehlt eine Datei, ohne geworfene Exception.
                Es sollte geprüft werden, ob das hinzufügen ohne disablen der Tabelle überhaupt funktioniert oder ob es Alternativen gibt
                (Mit höherem Tabellenintervall(10) gelöst?)

        -[S] Größenverhältnisse sind nicht Geräteunabhängig:
                -Tabellenspalten bzw. -Header wie bspw. "Dauer" der PlaylistView
                -ProgressIndicator
    -Logik:
        -[1] Eigene Genres werden nicht unterstützt (evtl. auf andere Bibliothek umsteigen)
        -[1] Nullpointer bei der Auswahl von Mp3FileData, wenn manche MP3-Eigenschaften nicht vorhanden sind
        -[1] Zugriffsrechte auf Dateien beim Speichern/Öffnen/Löschen
        -[1] NumberFormatException beim Sortieren der EditFile-Felder wenn noch nicht geladene Datei ausgewählt ist

#######################################################################################################################################################

TODO:
    -Projekt:
	-packages:
		-überarbeiten
        -pom: Nicht komplett src/main/resources includen (würde auch TODO und xmls einschließen)
        -Programm-Icon setzen
        -Dokumentation überarbeiten:
            -TODOs bei den Exceptions
            -Passend ausrichten (Absätze etc.) (<br>)
            -JavaDoc-Annotationen verwenden (z.B. für Verweise auf andere Mehtoden oder Konstanten)
            -Bei Methoden, die nur von einer Methoden aufgerufen werden, diese in der Dokumentation erwähnen?
    -GUI:
        -TreeTable-ähnliche Ansicht (vermutlich eigens geschriebene Komponente nötig)
        -Cover bei Auswahl (Mehrfachauswahl -> auf gleiches Cover prüfen)
        -manche Tabellenspalten sollten rechtsbündig sein
        -Vorbelegungen entfernen
        -(Mindest-)Größen für Textfelder, Labels und sonstige Elemente anpassen (bspw. splitpane). u.A.:
            -TextField-Labels in editFilePane mit mindestgröße versehen
            -Textfield größen anpassen (feste größe für tracknummer und jahr?)
        -[DONE?] Tabellenverhalten beim Laden von Verzeichnissen:
            -Tabelle disablen   oder
            -Dateien in Intervallen hinzufügen (Problem mit der ScrollBar -> Tabelle scrollt beim Hinzufügen mit)
        -Tabellennamen als Konstanten
    -Logik:
        -Listener für das Laden der ausgewählten Dateien nicht im EditFilePresenter
        -Speichern, Löschen und Öffnen von Wiedergabelisten
        -?TaskPool beim hinzufügen eines Tasks auch eine kleine Beschreibung mitgeben und auch mit speichern, damit man eine Liste davon abfragen kann
        -Tasks mit Cancel-if-Abfrage versehen (?? Evtl. nicht nötig, da das automatisch passiert?)
        -Wie soll der MainPresenter den Wiedergabe-Befehl an den MusicPlayer weitergeben? Direkt oder über die Playlist?
	-Restliche mp3-tag-infos beachten (z.B. Kommentar)